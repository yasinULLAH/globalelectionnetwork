{
  "commands": [
    "pkill -9 -f 'next build' 2>/dev/null; pkill -9 -f 'next-server' 2>/dev/null; sleep 2; cd /var/www/globalelectionnetwork && pm2 restart globalelection 2>&1; sleep 8; pm2 list 2>&1; curl -s -o /dev/null -w HTTP_%{http_code} http://localhost:3000"
  ]
}
